# Leave these unset to run Facemash on local demo data (the "compte de démonstration").
# Set them to run against the Supabase project: the app then reads and writes the
# `facemash` schema and signs users in with Supabase Auth.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
